const rows = 10;
const columns = 10;
const defaultCellSize = 40;

// Establece el tamaño mínimo y máximo de la celda
const minCellSize = 10;
const maxCellSize = 50;

export default { defaultCellSize, rows, columns, minCellSize, maxCellSize };
